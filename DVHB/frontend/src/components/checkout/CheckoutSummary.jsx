import PromoCodeInput from './PromoCodeInput';import OrderTotals from './OrderTotals';
export default function CheckoutSummary({items,onApplyCoupon,subtotal,discount,total}){return <aside className='card'><h2>Resumo do pedido</h2>{items.length===0?<p className='muted'>Nenhum item no carrinho. Adicione produtos para continuar.</p>:<ul className='summary-list'>{items.map(item=><li key={item.id||item.slug}><span>{item.name}</span><strong>{new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(item.price*item.quantity)}</strong></li>)}</ul>}<PromoCodeInput onApply={onApplyCoupon}/><OrderTotals subtotal={subtotal} discount={discount} total={total}/></aside>}

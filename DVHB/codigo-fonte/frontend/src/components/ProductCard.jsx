import { Link } from 'react-router-dom';
export default function ProductCard({p}){return <article className='card'><img src={p.imagem_principal || 'https://via.placeholder.com/120x120?text=Sem+Imagem'} alt={p.nome} style={{ width: '120px', height: '120px', objectFit: 'cover', borderRadius: '0.5rem', marginBottom: '1rem', display: 'block', marginLeft: 'auto', marginRight: 'auto' }} /><h3 style={{ textAlign: 'center' }}>{p.nome}</h3><p className='muted' style={{ textAlign: 'center' }}>{p.descricao}</p><p style={{ textAlign: 'center' }}><strong>Fornecedor Verificado</strong> · {p.rating || 0}★</p><p style={{ textAlign: 'center' }}><strong>R$ {p.preco},00/mês</strong></p><div style={{ display: 'flex', justifyContent: 'center' }}><Link className='btn btn-primary' to={`/produto/${p.id || p.slug}`}>Ver produto</Link></div></article>}
